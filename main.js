const arr = [1,2,3,4,5,6,,8,9]

/*const newArr = arr.map(function(item){
     return item * 2;
})

console.log(newArr)

const soma = arr.reduce(function(total, next){
    return total + next;
})

console.log(soma)

const filter = arr.filter(function(item){ //filtra os elementos
    return item %2 === 0; //Quando quer estritamente iguala a esse numero
})

console.log(filter) 

const find = arr.find(function(item){//encontra exatamente uma opção solicitada
    return item ===3;
})

console.log(find)

const newArr = arr.map(item => item * 2)
console.log(newArr)

const test = () => [1,2,3]
console.log (test)*/

function soma (a,b){
    return a+b;
}

console.log(soma(2,3))